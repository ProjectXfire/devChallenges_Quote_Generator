import React from "react";
// Styles
import { Loading as LoadingStyled, LoadingContainer } from "../styles/loading";

export const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingStyled />
    </LoadingContainer>
  );
};
